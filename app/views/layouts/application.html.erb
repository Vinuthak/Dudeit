<!DOCTYPE html>
<html>
  <head>
    <title>Dudeit</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

    <div id="content">
    <div id="login_logout">
    <% if @current_user %>
        Logged in as:
        <%= @current_user.name %>
        <em><%= link_to "(Logout)", session_path, data: { "turbo-method": :delete } %></em>
    <% else %>
        <em>Not logged in.</em>
        <%= link_to 'Login', new_session_path %>
    <% end %>
  </div>  

  <body>
  <div id="content"> 
      <h1>Dudeit</h1>
      <% unless flash[:notice].blank? %>
        <div id="notification"> <%= flash[:notice] %> </div>
      <% end %>
    <%= yield %>
    <ul id="navigation">
      <li><%= link_to 'List of stories', stories_path %></li>
      <li><%= link_to 'Submit a new story', new_story_path %></li>
    </ul>
  </body>
</html>
